#include <stdio.h>

int main() {
    int A, B, C, D;

    printf("Enter the four angles of the quadrilateral: ");
    scanf("%d %d %d %d", &A, &B, &C, &D);

    if ((A + C == 180) && (B + D == 180)) {
        printf("YES\n"); 
    } else {
        printf("NO\n");  
    }

    return 0;
}
