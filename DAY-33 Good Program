#include <stdio.h>

int main() {
    int N;

    printf("Enter the number of bits (N): ");
    scanf("%d", &N);

    if (N > 0 && N % 4 == 0) {
        printf("Good\n");
    } else {
        printf("Not Good\n");
    }

    return 0;
}
